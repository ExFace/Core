<?php
namespace exface\Core\Widgets;

/**
 * A card is a well visible (typically bordered) form mainly usefully in dashboards.
 * 
 * Just like a `Form`, a `Card` has `buttons` to add action for navigation or performing
 * actions upon the card's data.
 *
 * @author Andrej Kabachnik
 *        
 */
class Card extends Form
{
}